FROM node:20-alpine

WORKDIR /app

# Install deps once into the image (keeps dev runs fast)
COPY frontend/package*.json ./
RUN npm ci

# By default run Vite dev server; host/port nastavíme aj v compose
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173", "--strictPort"]
